/**

 * below is the given map helper functions
	  let func = n  => n*2;
	  let arr = [1,53,52,12,45];

	  * To return the doubled of el of arr 
	  arr.map(func);

 *below is the given filter function
	let func = agr => agr.length > 4;
   let arr = ["mridul" , "aman", "prakhar"];
   console.log(arr.filter(func));


 */