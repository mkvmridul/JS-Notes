//sets



//Maps
/**
 * Key - value
 * Maps and object are very similiar
 * though the keys in maps can be string, int, function etc
 */

/*
let m = new Map();
let k = 'a'; //you can take any data type for this key
m.set(k,'v');
console.log(m);
*/

/*
// passing arr to Map() constructor thus is will create the object by itself
let arr =  [['a','1'], ['b','2'], ['c','3'], ['d','4']]; //{"a" => "1", "b" => "2", "c" => "3", "d" => "4"}
let m = new Map(arr);
console.log(m);
for(let [key,val] of m.entries()){
	console.log(`${key} points to ${val}`);
}

*/